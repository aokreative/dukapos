# Copy to .env.local to point the app at the backend.
# Leave unset to run the app fully in simulation (automated sending + M-PESA
# billing are simulated locally, no server needed).
VITE_API_URL=http://localhost:8787
