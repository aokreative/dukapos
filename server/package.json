{
  "name": "duka-pos-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Duka POS platform backend — relays debt reminders (WhatsApp/SMS) and collects M-PESA subscription payments. Runs in simulation until credentials are set.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "@google/genai": "^2.11.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "pg": "^8.13.1"
  }
}
